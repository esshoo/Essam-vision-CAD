# V24.1 Manual Checklist

1. Load a heavy PDF.
2. Run `window.__essamV23.convertCurrentRegistry()`.
3. Run `window.__essamV24.diagnoseCurrentPageSvg()`.
4. Check `estimate.suspectedOutlierProblem`.
5. Run `window.__essamV24.showSvgPreview({ maxEntities: 25000 })`.
6. Compare raw mode only if needed.
